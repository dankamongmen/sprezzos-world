--- a/src/CImg.h
+++ b/src/CImg.h
@@ -243,8 +243,8 @@
 #define _cimg_redefine_False
 #endif
 #include <cstddef>
-#include "cv.h"
-#include "highgui.h"
+#include "highgui/highgui_c.h"
+#include "highgui/highgui.hpp"
 #endif
 
 // Configure LibPNG support.
--- a/src/Makefile
+++ b/src/Makefile
@@ -171,6 +171,8 @@ else
 # OPENCV_CFLAGS = -Dcimg_use_opencv -I$(USR)/include -I$(USR)/include/opencv
 # OPENCV_LDFLAGS = -lcv -lhighgui
 # OPENCV_LDFLAGS = -lopencv_core -lopencv_highgui   #-> Use this for OpenCV 2.2.0 !
+OPENCV_CFLAGS = -Dcimg_use_opencv -I$(USR)/include/opencv -I$(USR)/include/opencv2
+PENCV_LDFLAGS = -lopencv_core -lopencv_highgui
 endif
 
 # Flags to enable native support of most classical image file formats, using the GraphicsMagick++ library.
