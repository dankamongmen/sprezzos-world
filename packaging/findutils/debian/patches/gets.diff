diff -ur findutils-4.5.10/gnulib/lib/stdio.in.h find/gnulib/lib/stdio.in.h
--- findutils-4.5.10/gnulib/lib/stdio.in.h	2011-04-02 11:50:24.000000000 -0400
+++ find/gnulib/lib/stdio.in.h	2012-12-31 02:05:31.477417206 -0500
@@ -175,12 +175,6 @@
                  "use gnulib module fflush for portable POSIX compliance");
 #endif
 
-/* It is very rare that the developer ever has full control of stdin,
-   so any use of gets warrants an unconditional warning.  Assume it is
-   always declared, since it is required by C89.  */
-#undef gets
-_GL_WARN_ON_USE (gets, "gets is a security hole - use fgets instead");
-
 #if @GNULIB_FOPEN@
 # if @REPLACE_FOPEN@
 #  if !(defined __cplusplus && defined GNULIB_NAMESPACE)
