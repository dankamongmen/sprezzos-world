diff -ur aptitude-0.6.9.1/doc/en/aptitude.xml src/aptitude-0.6.9.1/doc/en/aptitude.xml
--- a/doc/en/aptitude.xml	2012-07-11 22:31:30.000000000 -0400
+++ b/doc/en/aptitude.xml	2012-10-23 07:53:29.121442569 -0400
@@ -10742,7 +10742,7 @@
 
 	    <seglistitem id='configAuto-Upgrade'>
 	      <seg><literal>Aptitude::Auto-Upgrade</literal></seg>
-	      <seg><literal>false</literal></seg>
+	      <seg><literal>true</literal></seg>
 	      <seg>
 		If this option is <literal>true</literal>,
 		&aptitude; will automatically flag all
@@ -12113,7 +12113,7 @@
 	    <seglistitem id='configAdvance-On-Action'>
 	      <seg><literal>Aptitude::UI::Advance-On-Action</literal></seg>
 
-	      <seg><literal>false</literal></seg>
+	      <seg><literal>true</literal></seg>
 
 	      <seg>
 		If this option is set to
@@ -12302,14 +12302,14 @@
 
 	      <seg>
 		If this option is set to
-		<literal>true</literal>, the menu bar will be
-		hidden while it is not in use.
+		<literal>false</literal>, the menu bar will be
+		visible while it is not in use.
 	      </seg>
 	    </seglistitem>
 
 	    <seglistitem id='configMinibuf-Download-Bar'>
 	      <seg><literal>Aptitude::UI::Minibuf-Download-Bar</literal></seg>
-	      <seg><literal>false</literal></seg>
+	      <seg><literal>true</literal></seg>
 
 	      <seg>
 		If this option is set to
@@ -12325,13 +12325,13 @@
 	    <seglistitem id='configMinibuf-Prompts'>
 	      <seg><literal>Aptitude::UI::Minibuf-Prompts</literal></seg>
 
-	      <seg><literal>false</literal></seg>
+	      <seg><literal>true</literal></seg>
 
 	      <seg>
-		If this option is <literal>true</literal>, some
+		If this option is <literal>false</literal>, some
 		prompts (such as yes/no and multiple-choice
-		prompts) will be displayed at the bottom of the
-		screen instead of in dialog boxes.
+		prompts) will be displayed in dialog boxes instead
+		of at the bottom of the screen.
 	      </seg>
 	    </seglistitem>
 
@@ -12424,7 +12424,7 @@
 
 	    <seglistitem id='configPrompt-On-Exit'>
 	      <seg><literal>Aptitude::UI::Prompt-On-Exit</literal></seg>
-	      <seg><literal>true</literal></seg>
+	      <seg><literal>false</literal></seg>
 
 	      <seg>
 		If this option is <literal>true</literal>,
diff -ur aptitude-0.6.9.1/src/apt_options.cc src/aptitude-0.6.9.1/src/apt_options.cc
--- a/src/apt_options.cc	2012-07-11 03:35:10.000000000 -0400
+++ b/src/apt_options.cc	2012-10-23 07:51:43.324755255 -0400
@@ -172,7 +172,7 @@
 		 "displayed in a single line at the bottom of "
 		 "the screen.  If not, prompts will be displayed "
 		 "as pop-up dialog boxes."),
-	      PACKAGE "::UI::Minibuf-Prompts", false),
+	      PACKAGE "::UI::Minibuf-Prompts", true),
   option_item(N_("Show partial search results (incremental search)"),
 	      N_("If this option is enabled, aptitude will "
 		 "perform searches within the package list as "
@@ -191,7 +191,7 @@
 	      N_("If this option is enabled, aptitude will "
 		 "not terminate until you confirm that you "
 		 "really want to quit."),
-	      PACKAGE "::UI::Prompt-On-Exit", true),
+	      PACKAGE "::UI::Prompt-On-Exit", false),
   option_item::radio(N_("Pause after downloading files"),
 		     N_("This option controls whether aptitude will wait for confirmation after a download before it goes ahead and installs packages."),
 		     PACKAGE "::UI::Pause-After-Download",
@@ -207,7 +207,7 @@
 	      N_("If this option is enabled, aptitude will display "
 		 "the status of ongoing downloads at the bottom of "
 		 "the screen, rather than opening a new view."),
-	      PACKAGE "::UI::Minibuf-Download-Bar", false),
+	      PACKAGE "::UI::Minibuf-Download-Bar", true),
   option_item(N_("Display the information area by default"),
 	      N_("If this option is enabled, the information "
 		 "area (the pane at the bottom of the screen) in the "
@@ -223,13 +223,13 @@
 		 "the information area (the pane at the bottom of the screen) "
 		 "listing the different displays of information that can be "
 		 "viewed there."),
-	      PACKAGE "::UI::InfoAreaTabs", false),
+	      PACKAGE "::UI::InfoAreaTabs", true),
   option_item(N_("Advance to the next item after changing the state of a package"),
 	      N_("If this option is enabled, then performing an "
 		 "action on a package (for instance, installing or "
 		 "removing it) will move the selection to the next "
 		 "package in the list."),
-	      PACKAGE "::UI::Advance-On-Action", false),
+	      PACKAGE "::UI::Advance-On-Action", true),
   option_item(N_("Automatically show why packages are broken"),
 	      N_("If this option is enabled, then highlighting a "
 		 "package that has broken dependencies will "
@@ -283,7 +283,7 @@
 	      N_("If this option is enabled, then on startup, "
 		 "aptitude will select all upgradable packages for "
 		 "upgrade."),
-	      PACKAGE "::Auto-Upgrade", false),
+	      PACKAGE "::Auto-Upgrade", true),
   option_item(N_("Remove obsolete package files after downloading new package lists"),
 	      N_("If this option is enabled, then after every "
 		 "install run, aptitude will delete from the package "
