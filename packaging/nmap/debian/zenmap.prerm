#!/bin/sh -e

# Debian nmapfe prerm
# LaMont Jones <lamont@debian.org>

case "$1" in
    remove|upgrade|deconfigure)
	;;

    failed-upgrade)
	;;

    *)
	echo "prerm called with unknown argument \`$1'" >&2
	exit 1
	;;
esac
#DEBHELPER#
exit 0
