# package uses JNI interface so needs to be linked to JVM
python-lucene binary: binary-or-shlib-defines-rpath

# false positive: it's just readme.html. Registering it with doc-base looks useless
python-lucene binary: possible-documentation-but-no-doc-base-registration
