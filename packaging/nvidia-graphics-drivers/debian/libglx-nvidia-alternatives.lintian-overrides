# For smooth upgrades nvidia-alternative needs to depend on this transitional
# package, otherwise the package manager tries to remove the old package
# before nvidia-alternative is installed.  This removal fails because the
# diversions cannot be removed, but they will be properly migrated when
# nvidia-alternative gets installed.
transitional-package-should-be-oldlibs-extra oldlibs/optional
