# The access file contains informations (passwords) that could allow users to
# get unauthorized access to the fwknop-server. Therefore permissions on this
# file have to be tighten.
non-standard-file-perm etc/fwknop/access.conf 0600 != 0644

