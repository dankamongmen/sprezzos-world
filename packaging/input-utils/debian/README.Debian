input-utils for Debian
----------------------

The 'input-send' daemon which sends input events to other hosts over
the network has been omitted from the package since it is probably not
as useful as the other utilities. The same goes for the associated
'input-recv' program.

 -- Marcus Better <marcus@better.se>, Tue, 10 Jan 2012 11:39:02 +0100
