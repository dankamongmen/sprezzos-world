Kopete for Debian
-----------------

* The statistics plugin requires the Qt4 SQLite 3 plugin, so install the
  libqt4-sql-sqlite package for it.
* The latex plugin (called also KopeteTeX) requires the `latex' and `dvips'
  executables: just install texlive-latex-base for both.

 -- Debian Qt/KDE Maintainers
