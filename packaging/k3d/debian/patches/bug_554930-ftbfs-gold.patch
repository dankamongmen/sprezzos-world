--- a/k3dsdk/CMakeLists.txt
+++ b/k3dsdk/CMakeLists.txt
@@ -64,6 +64,8 @@
 	k3dsdk-glew
 	k3dsdk-parallel
 	${K3D_BOOST_UNIT_TEST_FRAMEWORK_LIBS}
+        # For gold linker, for dl*() functions
+        "dl"
 	)
 
 IF(NOT WIN32)
