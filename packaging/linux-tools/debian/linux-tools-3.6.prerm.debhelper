
# Automatically added by dh_python2:
if which pyclean >/dev/null 2>&1; then
	pyclean -p linux-tools-3.6 
else
	dpkg -L linux-tools-3.6 | grep \.py$ | while read file
	do
		rm -f "${file}"[co] >/dev/null
  	done
fi

# End automatically added section
