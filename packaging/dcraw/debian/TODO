1) Since 8.78 dcraw has added i18n support. This needs to be included in the
   debian package. Since the build process is somewhat different, I need
   to rewrite the scripts I use to make the .deb package
