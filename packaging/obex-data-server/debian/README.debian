obex-data-server for debian
===========================
This package includes ods-server-test.py as ods-server to serve as a basic OBEX
server for receiving files over bluetooth, the script requires python-dbus and
python-gobject.

e.g. “ods-server opp /tmp” starts an obex object push server which will receive
files into /tmp
