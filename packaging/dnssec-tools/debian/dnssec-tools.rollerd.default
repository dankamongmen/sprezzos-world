# Defaults for dnssec-tools rollerd initscript
# sourced by /etc/init.d/rollerd
# installed at /etc/default/rollerd by the maintainer scripts

# Additional options that are passed to the Daemon.
#DAEMON_OPTS="-rrfile /etc/dnssec-tools/dnssec-tools.rollrec"
