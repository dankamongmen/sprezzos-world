#!/usr/bin/make -f
# -*- makefile -*-
# Sample debian/rules that uses debhelper.
# This file was originally written by Joey Hess and Craig Small.
# As a special exception, when this file is copied by dh-make into a
# dh-make output file, you may use that output file without restriction.
# This special exception was added by Craig Small in version 0.37 of dh-make.

# Uncomment this to turn on verbose mode.
export DH_VERBOSE=1

override_dh_shlibdeps:

override_dh_auto_configure:
	env LD_LIBRARY_PATH=/usr/local/lib CFLAGS="-I$(shell pwd)/libzfs" LDFLAGS="-L/usr/local/lib" ./configure --enable-zfs --build=x86_64-linux-gnu --disable-dependency-tracking --prefix=/usr --includedir=${prefix}/include --mandir=${prefix}/share/man --infodir=${prefix}/share/info --sysconfdir=/etc --localstatedir=/var --libexecdir=${prefix}/lib/growlight

%:
	dh $@  --with autotools-dev
