tools/telepathy-glib.supp
examples/README
examples/Makefile.am
# The rest of the examples are in subdirectories, which dh_installexamples
# can't do (without copying the whole directory, which we don't want either).
