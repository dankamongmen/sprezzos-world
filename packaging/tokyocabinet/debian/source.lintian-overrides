# Upstream does not release tar-balls in a scanable way, also 
# upstream already depreciated itself in favor of another library.
# So a watchfile would make no sense.
tokyocabinet source: debian-watch-file-is-missing
