#!/usr/bin/make -f
# -*- makefile -*-

# Uncomment this to turn on verbose mode.
#export DH_VERBOSE=1

%:
	dh $@ 

override_dh_auto_build:
	dh_auto_build -- docs

override_dh_auto_install:
	dh_auto_install -- PREFIX=/usr
	rm debian/git-extras/usr/bin/git-gh-pages
