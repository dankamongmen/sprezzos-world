# Source package managed with Git but currently not with pristine-tar.

[DEFAULT]
# use pristine-tar:
#pristine-tar = True

[git-dch]
# include 7 digits of the commit id in the changelog entry:
id-length = 7
