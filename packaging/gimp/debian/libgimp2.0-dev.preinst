#!/bin/sh

set -e
                                                                                
ver=2.0
prio=50

if [ "$1" = "install" ] || [ "$1" = "upgrade" ]; then
	update-alternatives --remove gimptool /usr/bin/gimptool-2.0
fi

#DEBHELPER#
