# this symlink seems not to be cleaned properly.
config.status
# cleaning up after autotools-dev
config.sub
libltdl/config.sub
# autogenerated files that should also be cleaned upstream but aren't
ODBCConfig/mclass*.cpp
DataManager/mclass*.cpp
