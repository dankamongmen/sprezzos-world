#!/bin/sh

export LANG=C.UTF-8
chvt 8
/sbin/growlight-wait
RES=$?
# Spin until we get HUP/KILL, indicating (hopefully) a successful run
while [ $RES -ne 129 -a $RES -ne 137 ] ; do
	chvt 8
	/sbin/growlight-wait
	RES=$?
done
if [ $RES -eq 129 ] ; then
	return 0
fi
return 1
