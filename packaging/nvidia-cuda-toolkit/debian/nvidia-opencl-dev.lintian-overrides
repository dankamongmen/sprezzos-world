# Adding this override file already makes the empty-binary-package warning
# go away.

# This package is empty because the headers and libraries are provided by
# other packages (opencl-headers, nvidia-libopencl1), but the OpenCL user
# should just Build-Depend on this package.
#nvidia-opencl-dev: empty-binary-package
