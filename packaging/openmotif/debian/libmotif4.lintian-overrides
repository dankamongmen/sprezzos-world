# Well the package actualle contains three libraries but none of
# them has a name identical to the package name - but at least
# the soversion is correct for all.  Splitting up the package into
# three would be an option to fix this lintian warning - but this
# deserves more than a QA upload
libmotif4: package-name-doesnt-match-sonames libMrm4 libUil4 libXm4
