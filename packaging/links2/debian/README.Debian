If you want links2 to work with svgalib you will have to setuid root
the binary using:

    dpkg-statoverride --update --add root root 4755 /usr/bin/links2

If you try to run it anyway, you will get the following error:

    svgalib: Cannot get I/O permissions.
