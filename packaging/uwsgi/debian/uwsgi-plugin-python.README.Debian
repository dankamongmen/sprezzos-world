Issue: ImportError: No module named site
========================================

In case when Virtualenv environment is specified with 'home' option,
make sure that Python interpreter and uWSGI Python plugin versions
are the same. For example, if virtualenv is using python2.6 interpreter,
provide option `plugin = python26` in uWSGI configuration or as a command line
option.
