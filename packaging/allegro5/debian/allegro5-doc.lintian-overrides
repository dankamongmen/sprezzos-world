# The warning is "cannot adjust line". The reason is
# not apparent and the manpages work.
allegro5-doc binary: manpage-has-errors-from-man

# The text files used for generating the manpages 
# are also used to generate html documentation, so
# the use of single "-" is correct. Hacking the manpage
# generation program to replace them could introduce
# other errors and is not worth the effort.
allegro5-doc binary: hyphen-used-as-minus-sign
