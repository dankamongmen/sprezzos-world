diff -ur g2/grub-core/gnulib/stdio.in.h grub-2.00/grub-core/gnulib/stdio.in.h
--- g2/grub-core/gnulib/stdio.in.h	2013-01-11 20:59:39.222171436 +0000
+++ grub-2.00/grub-core/gnulib/stdio.in.h	2010-12-01 14:45:43.000000000 +0000
@@ -140,8 +140,8 @@
 /* It is very rare that the developer ever has full control of stdin,
    so any use of gets warrants an unconditional warning.  Assume it is
    always declared, since it is required by C89.  */
-//#undef gets
-//_GL_WARN_ON_USE (gets, "gets is a security hole - use fgets instead");
+#undef gets
+_GL_WARN_ON_USE (gets, "gets is a security hole - use fgets instead");
 
 #if @GNULIB_FOPEN@
 # if @REPLACE_FOPEN@
