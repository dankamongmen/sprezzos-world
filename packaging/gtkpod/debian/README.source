gtkpod for Debian
-----------------

Patches are saved in the debian/patches directory and they will be applied
using the standard "debuild" command, while they will be removed (unpatching
the source) upon any rebuilding command or using "debuild clean" directly.
