#!/usr/bin/make -f

# Uncomment this to turn on verbose mode.
#export DH_VERBOSE=1

override_dh_auto_configure:
	./bootstrap
	dh_auto_configure -- --enable-pulse --disable-rsxs --enable-cec

override_dh_auto_install:
	$(MAKE) install DESTDIR=$(DEB_SRCDIR)/tmp

%:
	dh $@ 
