Fixes a bashism in multi-gpsmap example
--- kismet-2011-03-R2.orig/extra/old/multi-gpsmap.sh	2009-08-31 01:36:19.000000000 +0200
+++ kismet-2011-03-R2/extra/old/multi-gpsmap.sh	2012-02-15 14:49:05.000000000 +0100
@@ -6,7 +6,7 @@
 
 awk '/gps-point/ { if ( $3 ~ "source.*" ) { print $6" "$7 } else { print $5" "$6 }}' < $1 | while read line; do
 	eval $line
-	if [[ $lat != 0 ]]; then
+	if [ $lat != 0 ]; then
 		echo $lat
 		cat > /dev/null
 		break
