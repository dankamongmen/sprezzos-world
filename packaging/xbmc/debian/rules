#!/usr/bin/make -f

# Uncomment this to turn on verbose mode.
#export DH_VERBOSE=1

# FIXME we eventually want --enable-pulse
override_dh_auto_configure:
	./bootstrap
	dh_auto_configure -- --disable-rsxs

override_dh_auto_install:
	$(MAKE) install DESTDIR=debian/tmp

%:
	dh $@ 
