# libkipiplugins.so is no 'proper' shared library.  It's contents
# changes as needed. Upstream does not support any use beside
# for kipi-plugins Therefore we did not split out a
# lib and lib-dev pkgs.  For further discussion
# see bug report #324592
#
kipi-plugins: no-symbols-control-file
kipi-plugins: non-dev-pkg-with-shlib-symlink
kipi-plugins: package-name-doesnt-match-sonames
