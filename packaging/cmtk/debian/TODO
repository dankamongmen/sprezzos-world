# emacs: -*- mode: org; -*-

* LZMA checks and cmtkCompressedStream.h looking
  for absent in distribution lzmadec.h (seems to be coming from the
  original LZMA SDK)
** still misses mxml while building testing/libs/System
* package atlas and provide path to it
* For testing needs
 - data/testing from SVN (111 MB)
 - sqlite3
 - xvfb
