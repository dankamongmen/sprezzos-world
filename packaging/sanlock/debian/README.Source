There aren't any official tarballs. How to create one for debian:
-Download the tagged version via gitweb[1]
-Extract the archive
-Rename the directory to sanlock-VERSION
-Recompress the directory to a orig tarball


[1] http://git.fedorahosted.org/git/?p=sanlock.git;a=summary

