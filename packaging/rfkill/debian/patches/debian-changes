Origin: upstream, other, *
Description: Applied patches. See Vcs-* headers in debian/control.

--- rfkill-0.4.orig/rfkill.8
+++ rfkill-0.4/rfkill.8
@@ -17,7 +17,7 @@ Show rfkill's built-in help text.
 .B event
 Listen for rfkill events and display them on stdout.
 .TP
-.B list [type]
+.BI list " [type]"
 List the current state of all available rfkill-using devices,
 or just all of the given type.
 .TP
